name: countries_client
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.0 <3.0.0'

dependencies:
  ferry: ^0.8.0
  gql_http_link: ^0.3.2

dev_dependencies:
  pedantic: ^1.9.0
  ferry_generator: ^0.3.0
  build_runner: ^1.10.2
